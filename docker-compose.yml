version: '3.8'
services:
  web:
    image: hnreviewer:latest
    container_name: hnreviewer
    restart: unless-stopped
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
